/**
 * ScreenType is used to maintain a finite number of options
 * for the currentScreen variable in the globalVariables
 * pinia store.
 */
export enum ScreenType {
  TITLE,
  PROJECTS_INTRO,
  RESIDENCE_INTRO,
  RESIDENCE_TECH,
  RESIDENCE_GEN,
  RESIDENCE_INTERACTION
}
